using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Suelo : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject hijo;
    //public GameObject landManagerGO;
    //public GenerarTerreno landManagerScript;

    void Awake()
    {
        //landManagerGO = GameObject.Find("LandManager");
        //landManagerScript.ActualizarTarget(hijo);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
